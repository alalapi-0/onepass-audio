window.addEventListener("DOMContentLoaded", () => {
  const info = document.createElement("p");
  info.textContent = "Web UI 后续版本将提供音频预览、标注与上传等完整功能。";
  document.querySelector("main")?.appendChild(info);
});
