# OnePass Audio package initializer.
# 用途: 提供即将加入的音频清理与字幕处理模块的命名空间。
# 依赖: 仅依赖 Python 标准库。
# 示例: ``from onepass import loader``（模块后续补充）。

"""OnePass Audio 包的占位符模块。

此包稍后将包含 loader、retake、clean、segment、EDL、writers、markers、
aggregation 与 pipeline 等子模块，用于处理有重录段落的录音素材。
"""

__all__: list[str] = []
