# === 必填（不要提交到Git；复制成 vultr.env 并填写） ===
VULTR_API_KEY=your_vultr_api_key_here

# === 推荐默认 ===
VULTR_REGION=sgp                 # 示例：sgp(新加坡) / nrt(东京) / lax(洛杉矶) / fra(法兰克福) 等
VULTR_PLAN=vc2-2c-4gb            # 示例通用计算；若需GPU请自行改写并确保账号可用
VULTR_OS=ubuntu-22.04            # OS 标识（cloud-init可用）

# === SSH/远端目录 ===
SSH_USER=ubuntu
SSH_PRIVATE_KEY=C:\Users\YOU\.ssh\id_rsa
REMOTE_DIR=/home/ubuntu/onepass

# === 实例命名/标签 ===
INSTANCE_LABEL=onepass-asr
TAG=onepass

# === 超时与轮询 ===
CREATE_TIMEOUT_SEC=900
POLL_INTERVAL_SEC=8
