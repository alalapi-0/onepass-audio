# WireGuard client profile generated by PrivateTunnel tooling.
# Keep this file secret: it contains the private key for your device.

[Interface]
PrivateKey = {{CLIENT_PRIVATE_KEY}}
Address = {{CLIENT_ADDR}}
DNS = {{CLIENT_DNS}}
{{CLIENT_MTU_LINE}}

[Peer]
PublicKey = {{SERVER_PUBLIC_KEY}}
Endpoint = {{ENDPOINT}}
AllowedIPs = {{ALLOWED_IPS}}
PersistentKeepalive = {{KEEPALIVE}}
