#!/usr/bin/env bash
# legacy_adapter.sh -- Remote-side bridge for legacy VPS projects.
# This script currently serves as a placeholder to keep directory structure
# consistent. If you need remote preparation logic for your legacy project,
# extend this script accordingly.
set -euo pipefail

echo "[legacy-adapter] 当前未定义远端适配操作。请在旧项目中直接调用 python scripts/asr_batch.py。"
